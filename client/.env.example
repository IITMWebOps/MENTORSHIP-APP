# Backend origin only (no /api suffix)
# Local:  http://localhost:8000
# Prod (Cloudflare Pages): https://saathi-iitm.pages.dev
#   — /api is proxied to Render via functions/api/[[path]].js
VITE_API_URL=http://localhost:8000
